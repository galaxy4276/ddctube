extends layouts/main.pug

block content
  .form-container
    form(action=routes.upload, method="post")
      input(type="text", placeholder="Title", name="UpdateTitle")
      textarea(name="description", placeholder="UpdateDescription")
      input(type="submit", value="Update Video")
    a.form-container__link(href=routes.videos+routes.deleteVideo) Delete Video